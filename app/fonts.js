import { Anek_Latin } from "next/font/google";
import { Newsreader } from "next/font/google";

export const title = Newsreader({ display: "swap", subsets: ["latin"], weight: ["400", "600", "700"]})

export const body = Anek_Latin({ display: "swap", subsets: ["latin"], weight: ["300"]})
